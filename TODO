Fixes
=====

* refine elogd_secure(): allow syslog cap...
* rate limit internal log messages
* implement elogd fallback timeout in case of store write error -ENOSPC....
* Move to slist impl

Documentation
=============

* User guide and man page
* Integration guide
* Document source code

Features
========

* Drop duplicate log messages and replace them with a single synthetic one ?
* Kernel ring-buffer renders special printable characters as escaped hexadecimal
  sequences (such as `\x09' for a TAB): translate these as real printable
  characters ?
* Support for LTTng traces ?
* Compute daemon operational statistics ?
