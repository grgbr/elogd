* Check file system permissions for mqueue / syslog socket and /dev/kmsg. Review
with respect to CONFIG_ELOGD_SVC_GROUP and CONFIG_ELOGD_USER build / command
line options !

* make sure all printable characters are properly rendered (tab is actually
rendered as \x09)

* delay line flush to store by a configurable amount of seconds and try to flush
  by a configurable amount of bytes

* make initial messages are properly ordered on first loop iteration

* return error upon unknown user group at file creations ??

* drop duplicates ?

* refine rfc line generation / fill (relocate into pipeline logic)

* restructure build logic

* make max number of lines (or max number of lines per queue) <= IOV_MAX / 2
