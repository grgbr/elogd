Fixes
=====

* Review kernel device file capability requirements (see kern.c)
* Make sure sysctl kernel.printk_devkmsg is off (disable writes from userspace).
* Make sure sysctl kernel.dmesg_restrict = 1
* Move to slist impl
* Check file system permissions for mqueue / syslog socket and /dev/kmsg. Review
  with respect to CONFIG_ELOGD_STORE_GROUP and CONFIG_ELOGD_USER build / command
  line options !
* Return error upon unknown user group at file creations ??

Documentation
=============

* User guide and man page
* Integration guide
* Document source code

Features
========

* Drop duplicate log messages and replace them with a single synthetic one ?
* Kernel ring-buffer renders special printable characters as escaped hexadecimal
  sequences (such as `\x09' for a TAB): translate these as real printable
  characters ?
* Support for LTTng traces ?
* Compute daemon operational statistics ?
